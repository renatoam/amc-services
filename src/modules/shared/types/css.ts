export enum CSSPosition {
  inherit = 'inherit',
  initial = 'initial',
  revert = 'revert',
  unset = 'unset',
  relative = 'relative',
  absolute = 'absolute',
  fixed = 'fixed',
  static = 'static',
  sticky = 'sticky',
}

export enum HelperStatus {
  success = 'success',
  info = 'info',
  error = 'error',
  warning = 'warning'
}

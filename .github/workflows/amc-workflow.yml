name: AMC CI | Manage Workflows

on:
  push:
    branches: [main]

jobs:
  build:
    uses: ./.github/workflows/amc-build.yml
  release:
    needs: build
    uses: ./.github/workflows/amc-release.yml
  deploy:
    needs: release
    uses: ./.github/workflows/amc-deploy.yml
